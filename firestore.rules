rules_version = '2';

// 教学项目：允许所有读写（云函数使用 Admin SDK，不受此规则限制）
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
